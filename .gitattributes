# ==============================================================================
# Git attributes — line ending rules
# ==============================================================================

# Default: auto-detect
* text=auto

# Shell scripts must use LF (Unix) line endings for Docker / Linux
*.sh text eol=lf

# Dockerfile should also use LF
Dockerfile text eol=lf
